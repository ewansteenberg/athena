/* Reset default margin and padding for all elements */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Styles for the navigation bar */
nav {
    width: 100%;
    height: 90px;
    color: #ffffff;
    background-color: black;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 20px;
}

/* Styles for the unordered list */
ul {
    color: #eee;
    list-style: none;
    display: flex;
}

/* Styles for the "Athena" text */
#Athena {
    font-size: 25px;
    color: #ffffff;
}

/* Styles for list items */
ul li {
    display: inline-block;
    line-height: 90px;
}

/* Styles for navigation links */
ul li a {
    display: block;
    text-decoration: none;
    font-size: 14px;
    font-family: italic;
    color: #eee;
    padding: 0 20px;
    transition: color 0.3s ease;
}

/* Styles for navigation links on hover */
ul li a:hover {
    color: rgb(180, 4, 196);
}

body {
    margin: 0; /* Remove default body margin */
    height: 100vh; /* Set body height to 100% of viewport height */
    background-image: url("Newbandphoto.jpg"); /* Replace with the path to your image file */
    background-size: cover; /* Scale the image to cover the entire background */
    background-position: center; /* Center the image */
    background-repeat: no-repeat; /* Prevent image repetition */
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    margin: 0;
}
    main {
        flex: 1;
        color: #eee;
      }

      footer {
        margin-top: auto;
        background-color: rgb(0, 0, 0);
        color: white; 
        padding: 4px;
      }
      video{
        position: fixed;
        left: 510px; /* Adjust the value to change the distance from the right edge */
        top: 230px; /* Adjust the value to change the distance from the top edge */
    }
